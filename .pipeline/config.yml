general:
steps:
  mtaBuild:
    buildTarget: 'CF'
  cloudFoundryDeploy:
    deployTool: 'mtaDeployPlugin'
    deployType: 'standard'
    cloudFoundry:
      org: 'SAPDX_extensions'
      space: 'dev'
      credentialsId: 'CF_CREDENTIALSID'