general:
steps:
  mtaBuild:
    buildTarget: 'CF'
  cloudFoundryDeploy:
    deployTool: 'mtaDeployPlugin'
    deployType: 'standard'
    cloudFoundry:
      org: 'Org_cpl261'
      space: 'dev'
      credentialsId: 'CF_CREDENTIALSID'